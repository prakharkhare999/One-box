document.getElementById('login-form').addEventListener('submit', function(event) {
    event.preventDefault();
    // Perform login logic here
    window.location.href = 'onebox.html'; // Redirect to onebox screen after login
});
